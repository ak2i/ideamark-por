import type { RunConfig } from "../config.js";

// Placeholder: implemented in Phases 2-4 (chunking, LLM extraction,
// candidate assembly, draft emission, validation handoff).
export async function runGenerate(config: RunConfig): Promise<number> {
  void config;
  console.error("generate is not implemented yet (M1 Phases 2-4 in progress)");
  return 2;
}
